# Flat/hoisted node_modules so the one native dep (better-sqlite3) and
# electron-rebuild / electron-builder resolve correctly. This is the
# single config knob that neutralizes pnpm<->Electron friction (R4).
# See ADR-0009.
node-linker=hoisted
